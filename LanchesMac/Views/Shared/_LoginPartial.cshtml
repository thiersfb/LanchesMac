@using Microsoft.AspNetCore.Identity;

@inject SignInManager<IdentityUser> SignInManager

@if(SignInManager.IsSignedIn(User))
{
    <form asp-area="" asp-controller="Account" asp-action="Logout" id="logoutForm" method="post" class="navbar-left">
        <div class="dropdown">
            <a class="nav-link text-light dropdown-toggle" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false" asp-area="">Bem-vindo @User.Identity.Name </a>
            <ul class="dropdown-menu">
                <li class="dropdown-item">
                    <button type="submit" style="width:auto; color:black" class="dropdown-item" >Logout</button>
                    @*<a class="dropdown-item" asp-controller="Account" asp-action="Logout" asp-route="" style="color:black">Todos os Lanches</a>*@
                    @*<a class="dropdown-item" asp-controller="Account" asp-action="Logout" asp-route-categoria="" style="color:black">Logout</a>*@
                    @*<input class="dropdown-item" type="submit" style="color:black" value="Logout" />*@
                </li>

            </ul>  
        </div>      
    </form>

    @*<form asp-area="" asp-controller="Account" asp-action="Logout" id="logoutForm" method="post" class="navbar-left">
        <ul class="nav navbar-nav navbar-left">
            <a class="nav-link text-light" asp-area="" asp-controller="" asp-action="Index">Bem-vindo(a): @User.Identity.Name </a>
            <button type="submit" style="width:auto" class="nav-link btn btn-link py-0">Logout &nbsp;</button>
        </ul>        
    </form>*@
}
else 
{
    <ul class="nav navbar-nav navbar-left">
        <a class="nav-link text-light" asp-controller="Account" asp-action="Login" asp-route-returnUrl="@string.Format("{0}{1}", Context.Request.Path, Context.Request.QueryString)" style="color:white">&nbsp; Log in&nbsp;</a>
        <a class="nav-link text-light" asp-controller="Account" asp-action="Register" style="color:white">&nbsp; Registro&nbsp;&nbsp;&nbsp;</a>
    </ul>
}